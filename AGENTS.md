# Agent Instructions

## Agent skills

### Issue tracker

GitHub Issues (`eli-lind/absorb`) via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Canonical 2 category (`bug`, `enhancement`) and 6 state roles (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `icebox`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout (`CONTEXT.md` at root + `docs/adr/`). See `docs/agents/domain.md`.

### Memory

Repo-local Markdown entries staged in `docs/agents/memory/`. See `docs/agents/memory.md`.
